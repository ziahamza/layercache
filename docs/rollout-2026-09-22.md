# Operational and local rollout, September 22

This updates the September 12 evidence. Earlier rollout notes describe the state
at their dates, not the current deployment checklist.

## Completed and observed

- Shared Caddy ingress serves LayerCache, Manifest, Booker and Phone directly.
  Cloudflare provides DNS. Existing app hostnames remain; new services use
  explicit routes under `*.services.ziahamza.com`. Private services remain private.
- Registry maintenance failure reproduced against the real filesystem. Deleted
  Distribution revision links left empty directories, which crashed the scanner.
  PR #1 tolerates deletion remnants but fails closed on broken live references.
  Two independent reviews passed, CI passed, and two full production maintenance
  runs succeeded after merge and deployment.
- PR #2 adds `layercache login --github-cli`. The GitHub credential remains owned
  by GitHub CLI; only short-lived project capabilities and its executable path
  persist in LayerCache. Independent review and CI passed. The CLI is installed
  at `/home/hzia/.local/bin/layercache` on the personal host.
- Project-scoped local configurations now exist for GitEnv, Parle, Booker,
  Agent Access and Phone under `~/.config/layercache/<alias>.json`. Each local data directory
  is inside the bounded pool, with a 1 GiB logical budget and 10 GiB free reserve.
  All five authenticated against Team Cache. Using plain `pnpm` alone does not
  imply use of LayerCache; run it through `layercache run --config ...`.
- PR #3 installs read-only capacity, maintenance, report and certificate probes.
  The host timer is enabled and passes. The independent GitHub-hosted health
  probe passed in run `35690574060`. Host alerts remain local journal signals;
  GitHub notifications depend on account settings, not a configured pager.
- Config PR #24 provides persistent user services. The five project runtimes
  are active under systemd, with automatic short-lived credential refresh.
- Config PR #23 and LayerCache PR #4 provision Phone as a fifth isolated project
  inside the same bounded pool. Its local runtime is active on port 17440,
  authenticated status is healthy, and the host monitor includes the project.
  Phone CI PR #38 still requires its hosted qualification after merge.
- Parle PR #46 fixes Local CI's hardcoded project identity and explicitly
  forwards non-secret cache configuration. Host-loopback endpoints are rejected
  for bridged Local CI containers, with guidance to use the host-native wrapper.
  Its merged source `c879a5e` ran `typecheck:tools` cold in 4.620 seconds and
  repeated in 77 ms with one cache hit. This same-daemon result is not claimed
  as an independent Team Cache restore.

## Fresh-worktree acceptance

Agent Access source `6586ae4` was checked out into two disposable worktrees.
Dependencies were installed from the frozen lockfile, without lifecycle scripts.
Both used Node 24.13.0 and pnpm 9.12.0. The producer and consumer had separate
empty Local Cache directories and separate daemon configurations. Turbo's own
local cache was excluded with `--cache=remote:rw` for the producer and
`--cache=remote:r` for the consumer.

| Observation | Producer | Consumer |
| --- | --- | --- |
| Typecheck tasks | 5 successful | 5 successful |
| Cache hits | 0 | 5 |
| Turbo elapsed time | 7.119 s | 651 ms |
| Recorded source | none | Team Cache, all 5 |
| LayerCache run | `run-kCvbzFzmecSw5brmJgPB` | `run-rVGD7cKP-Y5NuPMHt5wB` |

The consumer report estimated 6.478 seconds of net critical-path time saved,
with timing known for all five tasks. Summed producer task duration was 9.020
seconds. These are task-duration measurements and an estimated build-time
benefit, not measured CPU seconds. Installation and runner startup are excluded.
This is one qualified workload, not a representative fleet-wide ROI estimate.
The two disposable acceptance worktrees and the independent reader cache were
removed after validation; the persistent project runtime remains installed.

## Scope still requiring evidence

- Docker transport and retention work, but consumer Docker build workflows still
  need explicit `cache-from`/`cache-to` rollout and a real repeated build test.
- Parle has prior evidence of a Linux native artifact hit skipping its Mac job.
  Booker still uses its existing native release/fingerprint workflow. Its local
  Expo provider is not proof of a CI-to-local simulator restore. Phone's native
  signing workflow must retain its existing release correctness checks.
- npm mirrors, Bazel rollout, Xcode compiler-cache integration, release repacking,
  OTA distribution and production Public Builds are not provided by this pass.
  A native artifact hit is not a full EAS replacement.
- Do not retire the retained tunnel based solely on health checks. Real app
  login/callback and authenticated streaming acceptance must precede removing
  that rollback option.
